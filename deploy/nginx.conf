# Personal website — serves the Wii menu (and the rest of the static site) at /.
# The Flask app handles /ttr itself (302 redirect to TTR_URL), so nginx just
# proxies everything to gunicorn on 127.0.0.1:5002.
server {
    listen 80 default_server;
    server_name _;

    client_max_body_size 20m;

    location / {
        proxy_pass http://127.0.0.1:5002;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# ---------------------------------------------------------------------------
# Ticket to Ride reachable host (the /ttr redirect target).
#
# The website redirects /ttr -> TTR_URL. TTR serves everything at its own root
# and speaks WebSockets, so it needs its OWN host (one IP + port 80 can't serve
# both apps at /). Two options:
#
#   1. Keep TTR where it already runs and set TTR_URL to that (e.g. its current
#      IP). Nothing to add here.
#   2. Run TTR on THIS box from the submodule (see deploy/ttr.service) and give
#      it a subdomain. Uncomment below, set the name, and set
#      TTR_URL=http://ttr.your-domain.com in .env.
# ---------------------------------------------------------------------------
# server {
#     listen 80;
#     server_name ttr.your-domain.com;
#
#     location / {
#         proxy_pass http://127.0.0.1:5001;
#         proxy_http_version 1.1;
#         # WebSocket upgrade headers — required for Socket.IO
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_read_timeout 86400;
#         proxy_send_timeout 86400;
#     }
# }
