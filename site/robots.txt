# https://www.robotstxt.org/robotstxt.html
#
# The landing page is the site, as far as search is concerned. Everything below
# is a page nothing links to - the tiles on `/` open modals or point at the game
# subdomains - and Chinmay does not want them found. They still answer if you
# know the address; this is about what is advertised, not what exists.
#
# The games are on their own hosts (drive/ttr/ers/kot.cgovind.com), each with
# its own robots.txt, and are discovered through the links on the landing page.
User-agent: *

# The old projects landing page, and everything reachable only through it.
Disallow: /home/
Disallow: /projects/
Disallow: /games/
Disallow: /channels/

# The Wii menu recreation.
Disallow: /wii/

Sitemap: https://cgovind.com/sitemap.xml
