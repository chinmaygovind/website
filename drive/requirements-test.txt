# Test-only dependencies. Kept out of requirements.txt on purpose: the box
# installs that one.
#
# QuickJS has moved into requirements.txt: `verify.py` runs the game's real
# JavaScript in production now, so it is not a test-only dependency any more.
# It is still what the tests that run trackmesh/physics/course use, and what
# test_touch.py / test_pending.py drive their stub browsers with - without it
# those tests skip, which looks like a pass.

# See the note in the root requirements-test.txt. Drive does not use it (see
# `parallel_for` in scripts/tests.sh); it is here so that a venv built from
# these files can.
pytest-xdist>=3.5
